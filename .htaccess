<IfModule mod_rewrite.c>
    RewriteEngine On

    # Hostinger serves the repository from public_html; Laravel's entry point
    # lives in the public directory.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
